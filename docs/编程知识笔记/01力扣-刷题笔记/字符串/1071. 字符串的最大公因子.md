# 难度：简单

# 题目
对于字符串 `s` 和 `t`，只有在 `s = t + ... + t`（`t` 自身连接 1 次或多次）时，我们才认定 “`t` 能除尽 `s`”。

给定两个字符串 `str1` 和 `str2` 。返回 _最长字符串 `x`，要求满足 `x` 能除尽 `str1`且 `x` 能除尽 `str2`_ 。

# 解题
## pypthon
```python
class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        for i in range(min(len(str1), len(str2)), 0, -1):
            if (len(str1) % i) == 0 and (len(str2) % i) == 0:
                if str1[: i] * (len(str1) // i) == str1 and str1[: i] * (len(str2) // i) == str2:
                    return str1[: i]
        return ''


```

## 我的python解法
```python
def solution(str1,str2):
    candidate=min(str1,str2)
    for i in range(len(candidate),0,-1):
        if len(str1)%i == 0 and len(str2)%i == 0:
            len1 = len(str1)//i
            len2 = len(str2)//i
            if str1[:i]*len1 == str1 and str2[:i]*len2 == str2:
                print(str1[:i])
                break  # 算最大公约数，需要break。在第一次出结果的时候就停止，才是最大公约数。继续往下计算会得出最小公约数。
    print('')

solution('abab','abababab')  
solution('abc','abcabcabcabc')

```


## cpp

```cpp
class Solution {
    bool check(string t,string s){
        int lenx = (int)s.length() / (int)t.length();
        string ans = "";
        for (int i = 1; i <= lenx; ++i){
            ans = ans + t;
        }
        return ans == s;
    }
public:
    string gcdOfStrings(string str1, string str2) {
        int len1 = (int)str1.length(), len2 = (int)str2.length();
        for (int i = min(len1, len2); i >= 1; --i){ // 从长度大的开始枚举
            if (len1 % i == 0 && len2 % i == 0){
                string X = str1.substr(0, i);
                if (check(X, str1) && check(X, str2)) return X;
            }
        }
        return "";
    }
};


```



## python 优化

gcd() 函数 是计算最大公约数函数，即直接使用函数计算。

```python
class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        candidate_len = math.gcd(len(str1), len(str2))
        candidate = str1[: candidate_len]
        if candidate * (len(str1) // candidate_len) == str1 and candidate * (len(str2) // candidate_len) == str2:
            return candidate
        return ''

作者：力扣官方题解
链接：https://leetcode.cn/problems/greatest-common-divisor-of-strings/
来源：力扣（LeetCode）
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。
```